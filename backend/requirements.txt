fastapi
uvicorn
python-multipart
qdrant-client
openai
# Generation via LangChain (LCEL + ChatOpenAI → vLLM).
# ponytail: langchain-core + langchain-openai ONLY. Not the langchain meta-package,
# langchain-community, or langchain-qdrant — native Qdrant RRF + FlagReranker + BGE-M3
# chunking are kept (LC wrappers do them worse). Bump both together.
langchain-core
langchain-openai
pypdf2
psycopg[binary]
python-dotenv
pandas
openpyxl
python-docx
tabulate
# v2: Supabase Storage (system of record for raw files)
supabase
# v2: BGE-M3 hybrid embeddings + reranker + tokenizer (local, runs on the DGX Spark GPU)
FlagEmbedding
# ponytail: pinned <5 — FlagEmbedding 1.4.0's reranker calls tokenizer.prepare_for_model,
# which transformers 5.x removed. Bump both together if you ever upgrade FlagEmbedding.
transformers<5
torch
# v2: web research — SearXNG search (httpx) + page→markdown extraction (no browser)
httpx
trafilatura
